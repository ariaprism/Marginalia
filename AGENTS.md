# Marginalia 当前施工状态

## 共读约定

- 用户称呼：小狐狸。
- AI 共读者称呼：小G（取自 GPT）。
- 这是小狐狸与小G共同建设的异步共读项目。

## 当前阶段

- Phase：Phase 2 — Cloud Ink / 云端墨水（`v0.2 Living Book` 已完成）
- 状态：进行中
- 当前目标：补齐同步前遗漏的数据边界；随后接通真实 Supabase 书籍与痕迹同步、私有文件传输、自动触发和跨设备恢复，完成 `v0.3 Cloud Ink`。

## 已完成

- [x] 建立长期施工地图 `MARGINALIA_CONSTRUCTION_MAP.md`
- [x] 建立根目录 `AGENTS.md`
- [x] 初始化 React + TypeScript + Vite 项目
- [x] 固定首批依赖版本并提交 lockfile
- [x] 建立基础测试与代码规范工具
- [x] 确定第一版暖色纸张设计 token 与字体策略
- [x] 完成书架与分页阅读器视觉原型
- [x] 写入原创模拟样书《雨夜书房》
- [x] 完成第二版手机书架与书籍“小房间”
- [x] 将模拟固定页升级为随排版重算的动态分页
- [x] 增加页眉、单条划线详情与三列阅读统计
- [x] 完成手机书架紧凑化与封面竖排方向调整
- [x] 在书籍小房间按章节顺序展开全部痕迹
- [x] 明确并模拟“最近停留”的阅读位置语义
- [x] 建立展示、功能与阅读正文三种字体角色，并接通正文宋体／黑体切换
- [x] 调整品牌题记、作者署名、中文衬线书架索引与书籍小房间的视觉锚点
- [x] 增加主页信息列表与三列封面书架切换
- [x] 将主页品牌标记改为轻量的豆沙色镂空印泥章
- [x] 实现正文连续句子点选、紧凑批注气泡、“留痕／重温”批注流程与划线／批注线型区分
- [x] 将正文划线详情整理为带时间戳与三点菜单的轻量痕迹卡
- [x] 支持同一句子连续留下、单独修订或抹去多条批注
- [x] 收口 Phase 1A 视觉原型检查点：书架、书籍小房间、分页阅读、句子选择与多条痕迹交互
- [x] 定义不依赖 React / 浏览器 / Supabase 的核心领域对象（Book、Chapter、Locator、ReadingProgress、Highlight、Annotation、Marginalia）
- [x] 撰写 `docs/EPUB_ANCHORING.md`，明确 CFI、章节结构、原文上下文与重锚定策略
- [x] 为核心领域对象与定位规则补充单元测试
- [x] 生成来源合法的测试 EPUB《雨夜书房》
- [x] 实现基于 JSZip 的真实 EPUB 解析器（元数据、目录、章节）
- [x] 接入 IndexedDB：持久化书籍、EPUB 原文件、章节、阅读位置、划线、批注与她留下的文字
- [x] 阅读器渲染真实 EPUB 章节，书架与书籍小房间读取本地书籍
- [x] 划线与批注写入 IndexedDB，刷新后仍在；示例书痕迹改为播种进同一张表
- [x] 调整阅读排版默认值、移动端摘录兼容、详情页轻量化与书架空白侧边栏入口
- [x] 接通阅读位置的保存恢复，并增加每书唯一手动折页；临时跳章不覆盖自动继续位置
- [x] 收细折页与批注交互：折页菜单随阅读顶栏一起收起，批注操作改为极简编辑图标
- [x] 完成藏书弹窗：EPUB 预解析、元数据修改、可选英文名、书内／图片／四色内部封面、整书架预览与确认后入库
- [x] 支持 EPUB 3 `cover-image` 与 EPUB 2 `meta name="cover"` 封面提取
- [x] 统一封面书名为横向自然断行，短书名不拆、长书名优先保持词语完整
- [x] 书架按用户最近主动打开倒序排列，并跨刷新保留次序
- [x] 为真实导入书增加二次确认的完整删除，级联清除 EPUB 与全部本地阅读数据
- [x] 将 M 印章侧边栏接成“名帖”，可设置双方称呼与共读者人称并用于批注署名
- [x] 修复移动折页后图标只变色不填充：有折页时始终保持实心点亮
- [x] 详情页与书内页边痕迹改用完整 Locator，跳到原句实际所在动态页
- [x] 移出书房与批注操作菜单支持点击空白处收回；长简介弹窗限制高度并内部滚动
- [x] 模拟书退出正式书架；首次进入显示空书房并引导藏入真实 EPUB
- [x] 关闭示例痕迹自动播种，定向清理旧 `rain-room` 孤儿数据；测试继续显式装载《雨夜书房》夹具
- [x] 接通“标记读完／从头重温”状态流转，并在末页继续后翻时展示轻量尾笺确认
- [x] 将单一名帖侧栏整理为“书房抽屉”，预留名帖、念头、来访、影子书与云端书房入口
- [x] 收静空书房引导与页尾，书架入口改为错落细线菜单
- [x] 增加本地持久化书籍置顶：长按或书籍菜单盖章置顶，置顶书不受最近打开排序影响
- [x] 修正桌面／手机书架细节：桌面隐藏简介、统一浅白阅读纸条、校正列表印章，封面书架不显示印章
- [x] 新导入书默认进入“想读”，只有首次翻开正文才转为“在读”；打开书籍简介不改变状态
- [x] 置顶组内部按最近主动打开排序；未置顶书始终排在全部置顶书之后
- [x] 每书唯一折页只在实际命中的动态页实心点亮，翻离后暗下，移折后仅新位置点亮
- [x] 收细主页导航与抽屉结构：统一菜单线条、精简品牌题头、下移英文题记，书房／抽屉入口分组并改为主界面承载；书架作为默认一级入口
- [x] 将主页侧栏入口改为纸页／抽屉拉手式门签，加深书房与抽屉分组题签，并统一六个入口的依次滑入节奏
- [x] 收窄书房抽屉、压低主页与目录题头、移除内部关闭按钮并补齐退场动画；抽屉功能页改用左右分置中英文页名的虚线页眉，移除内容区重复标题
- [x] 修正真实 EPUB 章首规则：隐藏与目录章名重复的原书开头标题，首字下沉只落到普通章节的首个正文段落，版权页等前置章节不下沉，同时保持 Locator 段落索引稳定
- [x] 完成 `App.tsx` 第一轮 feature 拆分：书架视图模型与组件、藏书弹窗及导入状态、书房抽屉及名帖状态、阅读工具面板与排版状态分别归位；分页、Locator、痕迹与折页核心编排暂留 App
- [x] 补齐 Phase 1B 架构、本地数据、验收故事、已知边界与非目标文档，并更新 README 当前阶段
- [x] 配置 GitHub Pages 项目路径与自动部署工作流：每次推送 `main` 先通过 test／lint／build，再发布 `dist`
- [x] 发布首个 GitHub Pages 验收站 `https://ariaprism.github.io/Marginalia/`，启用 HTTPS 并确认公网返回 200
- [x] 修正跨页长句上的折页定位：折页锚到当前页实际字符偏移；阅读主题、字号、行距、页边距与字体跨刷新保留，夜间阅读同步浏览器主题色
- [x] 完成真实设备与 GitHub Pages 的 `v0.2 Living Book` 验收，Phase 1B 正式收口
- [x] 建立 `v1.0` 共读交换契约：单书单章、批注与有限上下文、JSON 规范格式、Markdown 阅读视图及共读者回复模板
- [x] 接通书籍小房间“递一页给她／收回她的页边文字”，支持按章勾选批注、导出、回复预览与幂等写回本地页边文字
- [x] 用《雨夜书房》夹具覆盖章首／章末上下文、错误格式、回复写回、只读未回与重复导入
- [x] 修复共读回复只在书籍小房间显示、阅读页“重温批注”遗漏共读者文字；文件交换载荷移除 Locator、元素路径与字符偏移
- [x] 确认 `docs/SYNC_SEMANTICS.md`：本地第一写入、自动双向同步、最近停留与书房内容同步、设备排版偏好留本地、outbox、少量冲突、删除传播、恢复及云端状态
- [x] 完成 `docs/CLOUD_DATA_DESIGN.md`：Phase 2 远端表、稳定 ID、变化游标、本地 outbox／syncState、双向算法、冲突、RLS、私有 Storage 与分阶段验收设计
- [x] 升级 IndexedDB v2，新增与业务写入同一事务的 outbox 及按云端账号保存游标的 syncState
- [x] 建立不依赖 Supabase 的同步操作、待寄合并、推送／拉取状态机与确定性 fake remote
- [x] 用自动测试验证断网保留与重试、操作幂等、双设备独立新增合并、删除不复活及被覆盖操作整链出队
- [x] 校正施工地图、当前状态、架构、本地数据与 README 的 Phase 2 文档漂移，并明确正式站“可登录但尚未真实同步”的边界
- [x] 升级 IndexedDB v3：独立保存折页与名帖，迁移旧 localStorage 名帖和 v2 内嵌折页，并为既有本地书房提供幂等的首次待寄扫描
- [x] 接通真实 Supabase 第一段往返：登录账号可幂等推送／拉取名帖与书目元数据，云端 RPC 与 RLS 只接受本人数据，其他待寄内容不会被误确认
- [x] 整理待寄箱与首次连接保护：同对象只保留最新动作，云端书房按书／正文／阅读状态／痕迹显示人话摘要，新设备先取云端再合并以免空白名帖覆盖旧数据；旧示例残留与无意义待寄纸条使用单事务安全清理
- [x] 接通第二段真实结构化同步：章节、阅读位置、折页、划线、批注、页边文字和删除传播均可通过本人 RLS 双向往返；EPUB 原文件继续留待 Storage 阶段
- [x] 在正式站用现有一册书完成人工寄出：17 章、最近停留、折页、3 条划线与 1 条批注进入云端，待收摘要只剩 EPUB 原书
- [x] 在干净浏览器取回同一册书的书目、17 章、阅读状态与 4 笔痕迹；无 EPUB 原包时仍可由云端章节正常阅读，封面图片留待文件同步
- [x] 最近停留摘要不再直接展示固定长度的定位片段，改为从章节正文解析锚点所在的完整句子

## 正在进行

- [ ] 用一册真实 EPUB 完成首次人工往返：同章章首／章中／章末批注 → 导出给小鱼 → 导入回复 → 刷新恢复
- [ ] 接通私有文件传输、触发调度与完整恢复

## 下次开工：直接从这里继续

当小狐狸说“小G，我们接下来干嘛”时，无需重新盘视觉稿，也不要从已经完成的交换契约、同步语义或建库工作重新开始。直接按下面顺序推进：

1. 定向验证两台设备同时修改折页／批注以及完整删除时的时间决胜与不复活规则。
2. 上传和下载私有 EPUB／封面；随后接应用启动、网络恢复、回到前台、写入防抖和 Realtime 提醒。
3. 最后启用“立即收好”和“从云端恢复”，完成真实跨设备验收后形成 `v0.3 Cloud Ink`。
4. “念头”先保留说明壳，等明确它是独立随笔、待读入口还是书内痕迹聚合后再实现；“来访”以后直接消费真实 MCP session / visit events，不先制造假记录。

小狐狸不需要提前准备代码；遇到会改变产品语义的选择（例如上传入口的文案、解析失败的处理方式）再请小狐狸决定。

### 当前正式站的云端边界

- 邮箱门帖和登录状态已经真实连接 Supabase Auth。
- 登录后可手动执行“收好书页与痕迹”：它会真实 push／pull 名帖、书目、章节、阅读位置、折页和痕迹，重复操作不会生成副本；EPUB 原文件仍不会被确认出队。
- 完整“立即收好”和“从云端恢复”仍未启用；文件上传下载和恢复验收完成前不能暗示整间书房已经备份。
- 本地阅读和写批注仍然安全地保存在当前浏览器；当前阶段卸载浏览器数据或换设备还不能恢复完整书房。

## 痕迹持久化在哪

- `src/data/local/db.ts` — 打开库、事务与索引读取。连接按 `IDBFactory` 缓存，不要改回每次调用重开。
- `src/data/local/traceStore.ts` — 痕迹的读写。`loadTraces` 把三张表合成 `Trace[]`，`passageKey` 是主键，`nextTimestamp` 保证时间戳严格递增。
- `src/data/local/seedSampleTraces.ts` — 示例书痕迹的幂等播种，开关 `SEED_SAMPLE_TRACES`。
- `src/reader/sentenceAnchor.ts` — 句子与 Locator 的换算。阅读器渲染和持久层换算必须共用 `segmentChapter`，否则句子编号对不上。
- `src/reader/trace.ts` — `Trace` / `NoteEntry` 视图模型与时间格式化。

改动这几个文件时留意：`App.tsx` 里的痕迹状态是异步读回来的，测试需要 `waitFor`；每个测试用例换一个空 `IDBFactory`（见 `src/test/setup.ts`），不要让用例之间共用库。

## 后续待办

- [x] 建立 `docs/DECISIONS.md`
- [x] 补充核心领域对象与 EPUB 定位文档
- [x] 补充 Phase 1B 架构、本地数据、检查点与最小代码边界文档
- [x] 导入弹窗与 EPUB 封面提取
- [x] 阅读位置的保存与恢复（`ReadingProgress` + `Locator`），以及每书唯一手动折页
- [x] 关闭示例书痕迹播种（`SEED_SAMPLE_TRACES`）并定向清掉旧 `rain-room` 孤儿数据
- [x] 明确第一版的非目标边界

## 当前阻塞

- 无

## 已验证

- 2026-07-26：`npm test`（20 项通过）
- 2026-07-26：`npm run lint`
- 2026-07-26：`npm run build`
- 2026-07-27：`npm test`（65 项通过，连续三次稳定绿）
- 2026-07-27：`npm run build`、`npx oxlint`
- 2026-07-27：手动确认刷新浏览器后划线与批注仍在
- 2026-07-28：`npm test`（68 项通过）、`npm run build`、`npm run lint`
- 2026-07-28：阅读位置、唯一折页与浏览器重进恢复接通后，`npm test`（71 项通过）、`npm run build`、`npm run lint`
- 2026-07-28：折页菜单与批注入口收细后，`npm test`（71 项通过）、`npm run build`、`npm run lint`
- 2026-07-28：藏书弹窗、确认入库与 EPUB 封面提取接通后，`npm test`（75 项通过）、`npm run build`、`npm run lint`
- 2026-07-29：封面自然断行、最近打开排序、整书删除与名帖接通后，`npm test`（85 项通过）、`npm run build`、`npm run lint`
- 2026-07-29：折页状态、痕迹精确跳转与弹层收口修复后，`npm test`（87 项通过）、`npm run build`、`npm run lint`
- 2026-07-29：模拟书退出正式书架、空书房与显式测试夹具接通后，`npm test`（88 项通过）、`npm run build`、`npm run lint`
- 2026-07-30：新设备按 lockfile 完成 `npm ci`，`npm run build` 通过，Vite 8.1.1 开发服务器可正常启动
- 2026-07-30：读完与重温流程接通后，`bookStore` 10 项通过，`npm run build`、`npm run lint` 通过；全量 App 测试受既有测试夹具数据库初始化波动影响，待单独收口
- 2026-07-30：书房抽屉壳与渐入动画完成后，侧栏定向测试通过，`npm run build`、`npm run lint` 通过
- 2026-07-30：空书房、书架菜单与置顶印章交互收细后，`npm test`（90 项通过）、`npm run build`、`npm run lint`
- 2026-07-30：响应式书架与新书状态语义修正后，`npm test`（91 项通过）、`npm run build`、`npm run lint`；本地浏览器完成 1280×800 与 390×844 视口验收
- 2026-07-30：置顶组内排序与唯一折页点亮语义修正后，`npm test`（91 项通过）、`npm run build`、`npm run lint`
- 2026-08-02：主页题头、页脚题记、藏书弹窗上拉过渡与书房／抽屉分组导航完成后，定向侧栏测试、`npm run build`、`npm run lint` 通过；本地浏览器完成 1280×800 与 390×844 视口交互验收
- 2026-08-04：侧栏入口、分组层级、两组滑入动画、收窄退场、顶部留白与功能页虚线页眉收细后，侧栏交互测试和相关导入用例定向通过，`npm run build`、`npm run lint` 通过；全量测试 92 项中 91 项通过，唯一失败为并发验证时 EPUB 预解析等待超时，随后单独复跑通过
- 2026-08-04：真实 EPUB 重复章名与首字下沉语义修正后，正文提取与 Locator 定向测试 26 项通过，`npm test`（95 项通过）、`npm run build`、`npm run lint` 通过
- 2026-08-05：`App.tsx` 第一轮 feature 拆分后由 2269 行／约 103 KB 降至 1703 行／约 76 KB，`npm test`（95 项通过）、`npm run build`、`npm run lint` 通过
- 2026-08-05：Phase 1B 文档与 GitHub Pages 工作流接通后，`npm test`（95 项通过）、`npm run build`、`npm run lint` 通过；生产产物资源路径确认为 `/Marginalia/assets/...`
- 2026-08-05：修正 CI 中依赖本机时区的痕迹时间断言；GitHub Actions 的 95 项测试、lint、build、Pages 上传与部署全部通过，`https://ariaprism.github.io/Marginalia/` 公网返回 HTTP 200
- 2026-08-06：跨页折页字符锚点、阅读排版偏好持久化与夜间浏览器主题色联动完成后，`npm test`（97 项通过）、`npm run build`、`npm run lint` 通过
- 2026-08-18：共读交换契约与书籍小房间双入口接通后，`npm test`（101 项通过）、`npm run build`、`npm run lint` 通过；自动浏览器视觉验收因当前无可连接浏览器实例未执行
- 2026-08-21：真实共读往返发现并修复阅读页详情遗漏共读者文字，精简交换包的内部定位字段；`npm test`（101 项通过）、`npm run build`、`npm run lint` 通过。首次定向测试受既有 Vitest worker 启动超时影响未加载测试，随后全量复跑稳定通过
- 2026-08-21：IndexedDB outbox／syncState 与 fake remote 同步地基完成；定向 9 项及全量 `npm test -- --maxWorkers=1`（110 项通过）、`npm run build`、`npm run lint`、`git diff --check` 通过。默认并发定向测试曾在 worker 启动前超时，单 worker 稳定通过
- 2026-08-21：独立 Supabase 项目 `Marginalia`（新加坡）建立；9 张 public 表全部启用 RLS，安全顾问 0 警告，匿名无业务表权限，双账号事务验证只能读取本人书与变化流；私有 `library` bucket、三笔正式 migration、生成类型与可选前端客户端配置归档；`npm run build`、`npm run lint`、`npm test -- --maxWorkers=1`（110 项通过）
- 2026-08-22：GitHub Actions 写入 `SUPABASE_URL` variable 与 `SUPABASE_PUBLISHABLE_KEY` secret；云端书房正式／本地环境分流及入口完成，定向 25 项测试、`npm run build`、`npm run lint` 通过；本地全量 112 项中唯一既有书房进入阅读器用例受 20 秒超时影响，随后单独复跑 1.96 秒通过；首次 CI 发现同毫秒 outbox 测试依赖 IndexedDB 返回顺序，已改为按动作查找
- 2026-08-28：校正施工地图、AGENTS、架构、本地数据与 README 的 Phase 2 状态；补记折页、名帖、旧藏书首次待寄三个同步前遗漏，并明确正式站登录不等于数据已同步；`git diff --check` 通过（仅换行符提示）
- 2026-08-28：IndexedDB v3 将折页与自动阅读位置分库存放、名帖进入本地正式记录与 outbox，并增加旧书房首次待寄扫描；定向 19 项通过，`npm run build`、`npm run lint` 通过；全量 116 项中 115 项通过，唯一失败为既有书房进入阅读器用例 20 秒超时，随后单独复跑通过（1.94 秒）
- 2026-09-01：真实名帖／书目元数据 RPC、Supabase remote adapter、IndexedDB 写回与阶段按钮接通；定向 8 项通过，`npm run build`、`npm run lint`、云端 migration 与权限核验通过。匿名角色无 RPC 执行权，authenticated 有执行权，安全顾问仅保留与当前无密码登录无关的泄露密码保护提示。
- 2026-09-02：待寄箱改为同实体写入时替换旧动作，并在云端书房打开时整理历史重复项；新增按书、原书、正文、阅读状态与痕迹的人话摘要。名帖／书目首次同步改为先拉后合并，默认空白名帖让位于云端，真实本地编辑仍按时间决胜。进一步定位并修复旧示例清理跨两次数据库操作造成的测试换库竞态，同时清除其无意义待寄纸条；定向同步 15 项、相邻阅读器 2 项、本地书库 11 项、全量 `npm test -- --maxWorkers=1`（128 项）、`npm run build`、`npm run lint` 通过。
- 2026-09-05：章节、阅读位置、折页、划线、批注与页边文字的真实双向适配和云端 RPC 接通；真实数据库事务回滚验证 `security invoker`、authenticated／anon 权限、RLS 与重复操作幂等，安全顾问无新增问题。补齐各同步阶段完成后不重复扫描已收内容的回归保护；定向 25 项、最终全量 `npm test -- --maxWorkers=1`（132 项）、`npm run build`、`npm run lint`、`git diff --check` 通过。
- 2026-09-06：正式站一册真实书人工寄出后只剩 EPUB 待收；云端核对为 1 本书、17 章、1 个最近停留、1 个折页、3 条划线与 1 条批注，与界面“寄出 23 笔”完全一致。
- 2026-09-06：干净浏览器成功取回 25 笔结构化内容，刷新后书目、正文、折页、最近停留和痕迹均可使用；确认缺失项只有未同步的封面图片与 EPUB 原包。最近停留改为解析锚点所在完整句；定向 13 项与 2 项慢用例、全量 `npm test -- --maxWorkers=1`（133 项）、`npm run build`、`npm run lint` 通过。首次并发验证曾遇 worker 启动超时，随后单独与全量复跑稳定通过。

## 近期决定

- 2026-07-18：第一版核心格式为 EPUB。
- 2026-07-18：第一版只做滚动阅读，不做 PDF、OCR、多人或 AI 模型调用。
- 2026-07-18：用户称为“小狐狸”，AI 共读者称为“小G”。
- 2026-07-19：阅读器改为分页优先，常驻界面只保留正文与动态页码。
- 2026-07-19：阅读工具使用隐藏式顶栏与五区底栏，点击页面中部唤起。
- 2026-07-19：手机书架采用封面与书籍信息横排、最近停留独占下行的混合布局。
- 2026-07-19：点击封面进入书籍“小房间”，点击书籍信息直接继续阅读。
- 2026-07-20：“最近停留”表示用户最后阅读位置，不等同于最近划线或最近批注。
- 2026-07-20：小房间按正文顺序展示痕迹；书内痕迹面板按时间倒序展示。
- 2026-07-20：字体分为展示衬线、功能系统字体和可切换阅读正文三种角色。
- 2026-07-20：浏览器与未来 PWA 的默认主题色使用比页面略深的暖纸色 `#e5d7c3`。
- 2026-07-21：点击主页品牌区可在信息列表与三列封面书架之间切换。
- 2026-07-21：Phase 1A 视觉原型形成阶段检查点，下一步回到 Phase 0 剩余领域地基。
- 2026-07-22：下一施工顺序固定为“核心领域对象 → EPUB 稳定定位 → 合法测试 EPUB → 真实导入 → IndexedDB 持久化”。
- 2026-07-26：核心领域对象定义在 `src/domain/`，不依赖 React、浏览器或 Supabase。
- 2026-07-26：EPUB 解析使用 JSZip 在浏览器本地完成，不依赖后端。
- 2026-07-26：本地数据使用 IndexedDB 持久化，schema 包含 books、epubFiles、chapters、readingProgress、highlights、annotations、marginalia。
- 2026-07-26：EPUB 解析失败时提示“这本书暂时无法打开”，右侧提供小箭头展开查看具体报错；点击空白处消失。
- 2026-07-27：句子索引不进持久层。持久层只存 Locator，句子区间每次加载时由 `resolveLocator` 重新算出来。
- 2026-07-27：痕迹的主键是 `passageKey`（章节 + 元素路径 + 偏移 + 原文），同一句子的划线与多条批注归到同一条痕迹。
- 2026-07-27：原文已经找不回来的痕迹标记为 `drifted`，继续列在痕迹面板里但不提供跳转，不静默丢弃。
- 2026-07-27：示例书《雨夜书房》的痕迹改为首次运行时播种进 IndexedDB，与导入的真书共用同一条读写路径；开关是 `SEED_SAMPLE_TRACES`。
- 2026-07-27：痕迹时间戳严格递增（`nextTimestamp`），排序在 `createdAt` 相同时以 id 兜底，避免同毫秒写入的多条批注顺序不定。
- 2026-07-28：自动“上次读到”与手动折页分离。正常翻页推进自动位置；目录、痕迹与章节跳转先视为临时翻看；每本书只保留一个可移动折页。
- 2026-07-28：藏书先预解析、确认后入库；英文名不自动翻译；封面可在书内图片、用户图片与四色 Marginalia 内部封面间选择。
- 2026-07-29：封面断行只改变呈现，不改写书名；书架按主动打开时间排序；真实导入书完整删除；名帖作为本地常驻设置，不强制首次开屏填写。
- 2026-07-29：所有痕迹入口优先用 Locator 在动态分页后定位原句；定位漂移时才降级到章节首页。
- 2026-07-29：正式书架只展示真实入库书；《雨夜书房》和小鱼回复退到测试夹具，示例播种默认关闭。
- 2026-07-30：书籍置顶使用淡墨 M 印章表达；长按是快捷方式，小房间菜单提供显式入口；置顶排序独立于最近打开时间。
- 2026-07-30：新导入书先归入“想读”；进入书籍小房间只算查看简介，首次真正打开正文时才转为“在读”。
- 2026-07-30：置顶只划分书架优先组，组内仍按最近主动打开排序；折页是否实心只表示当前动态页正是唯一折页所在页。
- 2026-07-30：读完由末页尾笺或详情菜单显式确认；“从头重温”回到在读并重置自动阅读位置，但保留折页与全部痕迹。
- 2026-07-30：全局侧栏命名为“书房抽屉”；一级入口为名帖、念头、来访、影子书和云端书房，后四者当前只留说明壳。
- 2026-08-18：`v0.2 Living Book` 经真实设备验收后完成；Phase 2 先用版本化共读交换包验证未来 MCP 的读写语义，再设计本地优先同步并接入 Supabase。“念头”不抢在云端地基与共读契约之前实现。
- 2026-08-21：文件交换与未来 MCP 只向共读者提供短语义 ID、批注与有限正文，Locator 和重锚定由 Marginalia 内部处理；MCP 稳定后文件入口降级为备份／诊断能力，不再占据书籍主操作。
- 2026-08-21：确认本地优先自动双向同步语义；最近停留、书籍与痕迹、折页、名帖、置顶和最近打开跨设备同步，阅读排版偏好留本地；同一批注允许小鱼多次重访，第一版不增加仅 Wi-Fi 上传 EPUB。
- 2026-08-21：完成 Phase 2 云端数据设计；业务实体沿用本地稳定字符串 ID，所有远端行带 Auth 所有权，软删除配合单调变化游标，Realtime 只触发补拉；先以 fake remote 完成本地同步地基，再连接真实 Supabase。
- 2026-08-21：Marginalia 使用独立 Supabase 项目，不与 `tidal-notes` 混表；暂停的 `Bubble` 暂不删除。云端项目 ref 为 `dipstslcooovpyclouai`，区域为 `ap-southeast-1`。
- 2026-08-22：真实藏书同步只在 GitHub Pages 正式站启用；localhost 默认不连接真实云端，未来需要调试时使用同项目内受 RLS 隔离的测试账号。项目 URL 与 publishable key 由部署环境提供，不让用户在界面手填。
- 2026-09-01：真实同步分段开放；第一段按钮明确命名为“先收名帖与书目”，仅确认已真实写入的两类元数据，其余 outbox 保留。完整“立即收好”和恢复仍以后续全链验收为准。
- 2026-09-02：待寄状态面向用户按书房内容归类，不再展示会随翻页膨胀的原始操作流水数；首次绑定账号必须先读取云端，系统新建的默认名帖不能凭设备时间覆盖既有云端名帖。
- 2026-09-05：结构化书房先于 EPUB 文件开放真实同步；寄送按外键依赖顺序执行，删除与折页取消保存云端记号，批注修订单独推进更新时间。完整备份与恢复仍须等私有 EPUB 传输通过验收。

## GitHub 操作说明

- 小G的受限命令环境可能把外网请求导向 `127.0.0.1:9`。此时沙箱内的 `gh auth status` 会把无法连接 GitHub API 误报为 keyring token 无效，即使小狐狸已正常登录。
- 判断登录状态时，以获准正常联网环境中的 `gh auth status` 为准；如果那里显示 `Logged in to github.com account ariaprism`，不要让小狐狸重复登录。
- `git push` 同样使用获准联网环境。只有正常联网环境也明确报告认证失败时，才请小狐狸重新认证。
- 当前是小狐狸与小G直接共建的早期项目；已经充分验证、准备纳入当前基线的改动可以直接提交到 `main`。实验性、风险较高或需要保留评审边界的工作再使用功能分支。

## Agent 开工规则

- 开始前完整阅读 `MARGINALIA_CONSTRUCTION_MAP.md`。
- 记得称呼用户为“小狐狸”，自称“小G”。
- 不越过当前 Phase 大规模实现后续功能。
- 完成工作后更新本文件的状态与验证结果。
- 重大架构决定同步写入 `docs/DECISIONS.md`。
